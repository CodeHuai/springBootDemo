# 新建模块说明

本项目是 **Spring Boot 多模块 Maven 工程**:根目录 `pom.xml` 是父工程(`packaging=pom`),统一管理所有依赖版本;每个子目录是一个独立可运行的 Spring Boot 模块(`packaging=jar`)。

每次新建子模块,推荐用项目自带的脚本,几秒搞定;也可以手动建。下面两种方式都讲。

---

## 一、先记住这几条约定

不管用哪种方式,新模块都要遵守以下约定(和 `helloworld_demo` 完全一致):

| 项目 | 约定 |
|------|------|
| 父工程坐标 | `com.huai : spring-boot-demo : 1.0.0-SNAPSHOT` |
| 子模块 | **不写 `groupId`**(继承父的 `com.huai`),`version` 用 `1.0.0-SNAPSHOT`,`packaging` 为 `jar` |
| 包名 | `com.huai.<模块名>`(模块名用小写+下划线,如 `user_demo`) |
| 启动类 | 放在包 `com.huai.<模块名>` 下,类名是模块名转 PascalCase + `Application`,如 `user_demo` → `UserDemoApplication` |
| 依赖版本 | **统一由父 pom 的 `<dependencyManagement>` 管理**,子模块里写依赖时**不写 `<version>`** |
| 技术栈 | Java 1.8 + Spring Boot 2.6.13 |

> ⚠️ 新模块的依赖如果要写版本号,说明你忘了在父 pom 里登记——正确做法见文末「五、加新依赖」。

---

## 二、方式一:一键脚本(推荐)

脚本位置:`scripts/new-module.ps1`(PowerShell)。

**它自动帮你做完所有事**:生成 `pom.xml`、目录结构、启动类、测试类、`application.yml`,把 `<module>` 登记进根 pom,并**自动选一个不冲突的端口**(扫描已有模块的端口取最大值 +1)。

### 运行命令(在项目根目录,任意终端都能用)

```bash
powershell -ExecutionPolicy Bypass -File scripts\new-module.ps1 user_demo
```

把 `user_demo` 换成你的模块名即可。运行后你会看到:

```
== 模块 [user_demo] 已生成 ==
   启动类 : com.huai.user_demo.UserDemoApplication
   端口   : 10087  (context-path: /user_demo)
   位置   : ...\springBootDemo\user_demo

下一步: 回到 IDEA,在 Maven 工具窗点击 [刷新 / Reload All Maven Projects],新模块即可被识别。
```

生成的目录结构:

```
user_demo/
├── pom.xml
└── src/
    ├── main/
    │   ├── java/com/huai/user_demo/
    │   │   └── UserDemoApplication.java
    │   └── resources/
    │       └── application.yml
    └── test/
        └── java/com/huai/user_demo/
            └── UserDemoApplicationTests.java
```

### 模块名规则

- 只能用**小写字母、数字、下划线**,且以字母开头(参考 `helloworld_demo`)。
- 脚本会校验:目录已存在、根 pom 已有同名模块时都会拒绝,不会覆盖。

### 关于执行策略(ExecutionPolicy)

Windows 默认禁止运行 `.ps1` 脚本,所以命令里带了 `-ExecutionPolicy Bypass`(只对本次运行生效,不改系统设置)。

如果想以后直接 `.\scripts\new-module.ps1 user_demo` 跑,可一次性放开当前用户权限(任选一种 PowerShell 终端执行一次):

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

### 跑完后别忘了

在 IDEA 右侧 **Maven 工具窗**点刷新(Reload All Maven Projects),新模块才会被识别、才能运行。

---

## 三、方式二:手动创建

不想用脚本时,照下面三步做(以 `user_demo` 为例)。

### 步骤 1:在根 `pom.xml` 登记

`<modules>` 里加一行:

```xml
<modules>
    <module>helloworld_demo</module>
    <module>user_demo</module>   <!-- 新增 -->
</modules>
```

### 步骤 2:建 `user_demo/pom.xml`

照搬下面的模板(关键:**不写 `groupId`**、**依赖都不写版本**):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>user_demo</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>user_demo</name>
    <description>user_demo 模块</description>

    <parent>
        <groupId>com.huai</groupId>
        <artifactId>spring-boot-demo</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <properties>
        <java.version>1.8</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>user_demo</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

### 步骤 3:建目录、启动类、配置文件

```
user_demo/src/main/java/com/huai/user_demo/UserDemoApplication.java
user_demo/src/main/resources/application.yml
user_demo/src/test/java/com/huai/user_demo/                  (放测试类)
```

启动类(第一行必须有 `package`,见下方避坑):

```java
package com.huai.user_demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class UserDemoApplication {
    public static void main(String[] args) {
        SpringApplication.run(UserDemoApplication.class, args);
    }
}
```

`application.yml`(**端口要和别的模块错开**):

```yaml
server:
  port: 10087
  servlet:
    context-path: /user_demo
```

---

## 四、踩过的坑(重点)

### 1.「Spring Boot Application in default package」警告

意思是启动类被放在了**默认包**——也就是类文件**没有 `package` 声明**,或被直接建在 `src/main/java` 根目录下。

**为什么不行**:`@SpringBootApplication` 会从启动类所在包开始扫描组件;在默认包会扫描整个 classpath,容易引发 Bean 冲突、拖慢启动。

**解决**:启动类必须放在 `com/huai/<模块名>/` 目录下,且文件第一行是 `package com.huai.<模块名>;`。用脚本生成不会犯这个错。

### 2. IDEA 里 New Module 选什么

`New → Module` 向导里:

- **Generators / Create from archetype** 选 `Maven Archetype`,JDK 选 `1.8`。
- **Archetype** 选 `maven-archetype-quickstart`(纯 Java 骨架)。
- ❌ 别选 `maven-archetype-webapp`(那是 WAR 工程,会多出无用的 webapp 结构)。
- ❌ 别选 `Spring Initializr`(它会生成独立根 pom,不适合做子模块)。
- 填 GroupId=`com.huai`、ArtifactId=模块名、Version=`1.0.0-SNAPSHOT`。

> 用 archetype 建完后,生成的 pom 是「独立项目」形态,**还要手动**加上面的 `<parent>`、删掉自带的旧版 junit、确认根 pom 的 `<modules>` 已登记——所以**不如直接用脚本**。

### 3. Spring Boot 版本对不上

本项目用的是 **Spring Boot 2.6.13 + Java 8**,而网上九成新教程是 **Spring Boot 3.x + Java 17/21**(`javax`→`jakarta`、Security 配置写法、部分 starter 名字都变了),照抄会直接报错。

**对策**:依赖版本以本项目父 pom 为准,别从博客复制版本号;查用法时注意对应 2.x 文档。

### 4. 端口冲突

每个可运行模块的 `server.port` 必须唯一。`helloworld_demo` 占了 `10086`。脚本会自动避让;手动建时记得换一个。

---

## 五、加新依赖的正确姿势

以加 `mysql-connector-java` 为例:

1. **先在根 `pom.xml`** 的 `<dependencyManagement>` 里登记版本(版本变量放 `<properties>`):

   ```xml
   <!-- properties -->
   <mysql.version>8.0.21</mysql.version>

   <!-- dependencyManagement -->
   <dependency>
       <groupId>mysql</groupId>
       <artifactId>mysql-connector-java</artifactId>
       <version>${mysql.version}</version>
   </dependency>
   ```

2. **再在需要它的子模块 `pom.xml`** 里引入(**不写 version**):

   ```xml
   <dependency>
       <groupId>mysql</groupId>
       <artifactId>mysql-connector-java</artifactId>
   </dependency>
   ```

这样所有模块版本统一,升级时只改父 pom 一处。
