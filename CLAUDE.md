# 项目协作约定

## 纠错记录原则

项目根目录的 `TROUBLESHOOTING.md` 用于沉淀排查过的报错与 bug。**是否记录由用户决定**：

- 默认**不**主动写入
- 用户明确要求记录某个问题时才追加；拿不准要不要记，先问用户
- 用户明确要求删除/调整条目时照办

记录要素（保持文件既有格式，只追加不覆盖）：**模块**（运行哪个模块时出的错）→ **现象**（报错原文或错误行为）→ **复现**（怎样操作触发的）→ **根因** → **修复** → **教训**（一句话）。

- 复现路径不明确时，先问用户是怎么操作出来的，问清后一并记录
- 用户偶尔翻看这个文件复盘，条目要写得让一个月后的自己也能看懂